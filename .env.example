# Local development only. This file is an EXAMPLE and carries no real value.
# The production credential is the GitHub secret FOOTBALL_ODDS_API_KEY.
# A real .env is gitignored and must never be committed.
#
# Same Odds API account and the same credit pool as the NHL lab. Spending
# here spends there.
FOOTBALL_ODDS_API_KEY=
